def handle_error(error_message):
    """
    Handle errors by logging the error message.

    :param error_message: The error message to be logged.
    """
    # Log the error message (could be extended to log to a file or other logging system)
    print(error_message)
